# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Settings and env
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
email-validator==2.3.0        # backs pydantic's EmailStr

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9        # PostgreSQL driver, production only

# Auth
python-jose[cryptography]==3.3.0
# bcrypt is used directly, not through passlib: passlib 1.7.x reads
# bcrypt.__about__.__version__, which bcrypt 4+ removed, so the pair
# raises at hash time. See backend/utils/auth_utils.py.
bcrypt==5.0.0

# File handling
python-multipart==0.0.9
nbformat==5.10.4
beautifulsoup4==4.12.3
lxml==5.2.2

# AI — one of these backs grading, chosen by LLM_PROVIDER in .env.
# openai also drives local OpenAI-compatible servers (vLLM, Ollama, LM Studio).
openai==3.7.0
anthropic==1.2.0

# Data and export
pandas==2.2.2
reportlab==4.1.0
openpyxl==3.1.2

# HTTP client
httpx==0.27.0                 # used by Canvas integration + TestClient
requests==2.34.2

# Streamlit frontend
streamlit==1.35.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7

# Utilities
python-slugify==8.0.4
