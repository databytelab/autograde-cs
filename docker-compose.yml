# Docker Compose — populated in Stage 13
version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env

  frontend:
    build: ./frontend_streamlit
    ports:
      - "8501:8501"
    depends_on:
      - backend
